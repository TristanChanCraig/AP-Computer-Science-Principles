#include <iostream>
#include <math.h>
using namespace std;
void reverse(string& s) {
    for (int i = 0; i < s.length()-1; i++) {
        for (int j = 0; j < s.length()-1; j++) {
            if (s[j] == ' ') {
                
            }
        }
    }
}

int main() {
    string sen;
    printf("Enter sentence: ");
    getline(cin, sen);
    
    cout << sen;    
}